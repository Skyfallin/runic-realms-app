import { Box } from "@chakra-ui/react"
import Head from "next/head"
import React from "react"
import { ImageCarousel } from "~/components/image-carousel"
import LogoContent from "~/components/logo-content"
import { Navigation } from "~/components/navigation"

const Home = () => {
  const [activeIndex, setActiveIndex] = React.useState(0)

  return (
    <>
      <Head>
        <title>Create T3 AppTest</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="background portal-grid grid min-h-screen grid-cols-2 grid-rows-2">
        <Box className="logo-content-container">
          {/* bg-green-500 opacity-50 */}
          <LogoContent />
        </Box>
        <Box position="relative">
          <ImageCarousel />
        </Box>
        <Box display="flex" className="footer" flexDirection={"column"}></Box>
        <Navigation activeIndex={activeIndex} setActiveIndex={setActiveIndex} />
      </main>
    </>
  )
}

export default Home
